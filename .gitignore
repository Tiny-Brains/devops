# Filled-in values: client secrets, session keys, database passwords. The template is .env.example.
.env
.env.local
compose/orion/orion.local.toml

# The plugin trust root -- scripts/setup/trust-keygen.sh writes it. Never committed: the server
# only verifies, so a key beside the thing it signs proves nothing.
keys/
